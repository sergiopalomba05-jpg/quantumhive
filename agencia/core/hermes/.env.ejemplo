# Plantilla de variables de Hermes para QuantumHive (SIN keys reales).
# La real vive SOLO en %LOCALAPPDATA%\hermes\.env (fuera del repo, nunca se commitea).

# --- Cerebro: Google Gemini (AI Studio) ---
GOOGLE_API_KEY=

# --- Telegram (el bot responde SOLO a Sergio) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=

# --- (Opcional) STT por Groq en vez de faster-whisper local ---
# GROQ_API_KEY=
