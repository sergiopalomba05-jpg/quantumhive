# agencia/clientes — Clientes de la agencia

Solo **config + perfil + `.env`** por cliente. El código del producto vive en `agencia/productos/`.

- `yas-papeo/` — primer cliente.
- *(futuro)* `bazar/`, `logistica/`.
