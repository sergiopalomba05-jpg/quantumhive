import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quantumhive.directimport',
  appName: 'Directimport',
  webDir: 'dist'
};

export default config;
