# Entorno y secretos
.env
.env.local
.env.*.local
*.pem
agencia/agentes/**/.env
agencia/bots/**/.env

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
.next/
dist/
build/
.capacitor/
android/
ios/
*.tsbuildinfo

# IDEs y OS
.obsidian/
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Dependencias
.pnp/
.pnp.js
.yarn/
yarn.lock

# Supabase
supabase/.branches
supabase/.temp

# Capacitor
capacitor.config.json

# OpenCode
.opencode/plugin/
.codex/

# Temporales locales de subida/sincronizacion
.tmp.driveupload/

# Harness de tests (staging keys + captura de esquema vivo)
directimport/db/.env
directimport/db/_pedidos_vivo.txt

# Backups locales de prod (contienen PII de compradores) — JAMÁS al repo
directimport/db/_backup_prod_*/
directimport/db/.ultimo_backup.txt
# Temporales con secretos (tokens, passwords) — JAMÁS al repo
directimport/db/*.tmp

# graphify: trackear el mapa util para trabajar desde GitHub; ignorar cache/localpaths generados
graphify-out/*
!graphify-out/.graphify_labels.json
!graphify-out/GRAPH_REPORT.md
!graphify-out/cost.json
!graphify-out/graph.html
!graphify-out/graph.json
!graphify-out/manifest.json
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/20*/
graphify-out/cache/

# Python
.venv/
venv/
**/__pycache__/
*.pyc
.pytest_cache/
# Memoria local del bot (SQLite) — no al repo
agencia/clientes/*/cata.db
agencia/clientes/*/*.db

# PWA del catálogo: config local con la key publishable — no al repo
agencia/productos/catalogo-pwa/config.js

# Credenciales / conexión a VM (repo público — JAMÁS al repo)
VM/

# Dependencias instaladas (no al repo)
agencia/motor madre/paperclip-ai/
agencia/motor madre/openjarvis/
agencia/motor madre/ai-town/
